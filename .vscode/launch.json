{
  "configurations": [
    {
      "name": "Containers: Node.js Launch",
      "type": "docker",
      "request": "launch",
      "preLaunchTask": "docker-run: debug",
      "platform": "node",
      "node": {
        "package": "${workspaceFolder}/plantcare-mobile/package.json",
        "localRoot": "${workspaceFolder}/plantcare-mobile"
      }
    }
  ]
}
